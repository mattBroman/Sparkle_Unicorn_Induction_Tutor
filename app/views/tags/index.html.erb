
<% if not @tags.empty? %>
  <%= render 'partials/tables/tags/teacher' %>
<% else %>
    <p>You don't have any tags! make some.</p>
<% end %>
<div class="container">
  <%= link_to 'New Tag', new_tag_path, class: "btn btn-primary"%>
</div>
